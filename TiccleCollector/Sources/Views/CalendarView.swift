import SwiftUI

struct CalendarView: View {
    @ObservedObject var viewModel: BudgetViewModel
    
    var body: some View {
        Text("Calendar View - Coming Soon")
            .navigationTitle("달력")
    }
} 